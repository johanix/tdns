## tdns-cli auth daemon stop

Stop the management daemon

### Synopsis

Stop the management daemon. If it was not running, then this is a no-op.

```
tdns-cli auth daemon stop [flags]
```

### Options

```
  -h, --help   help for stop
```

### Options inherited from parent commands

```
      --config string   config file (default is /etc/tdns/tdns-cli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name
```

### SEE ALSO

* [tdns-cli auth daemon](tdns-cli_auth_daemon.md)	 - Only useful via sub-commands

