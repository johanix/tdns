## tdns-cli agent truststore sig0 delete

Delete a SIG(0) public key from the truststore

```
tdns-cli agent truststore sig0 delete [flags]
```

### Options

```
  -h, --help        help for delete
      --keyid int   Key ID of key to delete
```

### Options inherited from parent commands

```
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is /etc/tdns/tdns-cli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name
```

### SEE ALSO

* [tdns-cli agent truststore sig0](tdns-cli_agent_truststore_sig0.md)	 - Prefix command, only usable via sub-commands

