## tdns-cli auth notify

The 'notify' command is only usable via defined sub-commands

### Options

```
  -h, --help   help for notify
```

### Options inherited from parent commands

```
      --config string   config file (default is /etc/tdns/tdns-cli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name
```

### SEE ALSO

* [tdns-cli auth](tdns-cli_auth.md)	 - Interact with tdns-auth (authoritative) via API
* [tdns-cli auth notify send](tdns-cli_auth_notify_send.md)	 - The 'notify send' command is only usable via defined sub-commands

